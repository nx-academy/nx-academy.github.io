import type { Breadcrumb } from "../../types/Breadcrumb"

type Links = {
    previousNextChapterLink: string
    // fullNextChapterLink: string
    // breadcrumbs: Breadcrumb[]
}

const BASE_URL = "/cours/docker-et-docker-compose/chapitres"

export function handleLinks(url: string) {
    return url    
}
