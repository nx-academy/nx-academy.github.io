---
import BaseLayout from "../layouts/BaseLayout.astro"

const pageTitle = "Changelog"
const pageDescription = "NX Academy est regulièrement mis à jour. J'utilise cette page pour vous tenir au courant des derniers fixes et fonctionnalités."

import { ALL_MONTHS } from "../data/changelog"
---

<BaseLayout title={pageTitle} description={pageDescription}>
    <main class="main-wrapper">
        <h1>Changelog</h1>
        <div class="changelog-description">
            <p>
                Merci de prendre le temps de consulter ce changelog. Si le terme ne vous est pas familier, sachez qu’il s’agit simplement <strong>d’un journal de mise à jour</strong>. L’idée ici est de vous tenir au courant des évolutions de NX Academy : nouvelles fonctionnalités, corrections, ajustements.
            </p>
            <p>Chaque item est reliée à une pastille de couleur :</p>
            <ul class="indicators-list">
                <li>🟢 <strong>Vert</strong> pour les ajouts</li>
                <li>🟡 <strong>Jaune</strong> pour les mises à jour en cours</li>
                <li>🔴 <strong>Rouge</strong> pour les corrections de bugs</li>
            </ul>
    
            <p>Bonne lecture 🙂 !</p>
        </div>
        {
            ALL_MONTHS.map(month => (
                <section class="montly-section">
                    <h2>{month.monthName}</h2>
                    {
                        month.dailyLogs.map(dailyLog => (
                            <section class="daily-section">
                                <h3>{dailyLog.date}</h3>
                                <ul class="logs-list">
                                    {
                                        dailyLog.tasks.map(task => (
                                            <li class={`${task.kind}`}>{task.content}</li>
                                        ))
                                    }
                                </ul>
                            </section>
                        ))
                    }
                </section>
            ))
        }
    </main>
</BaseLayout>



<style>
    .main-wrapper {
        width: 90%;
        margin-left: auto;
        margin-right: auto;
        max-width: 1100px;
        max-inline-size: 66ch;
    }

    .changelog-description {
        margin-bottom: 32px;
        border-bottom: 1px solid #333;
    }

    h1 {
        margin-top: 0;
    }

    h2 {
        margin-bottom: 24px;
    }

    h3 {
        margin-bottom: 8px;
    }

    p {
        margin-bottom: 24px;
    }

    .indicators-list {
        margin-bottom: 32px;
    }

    .montly-section {
        margin-bottom: 80px;
    }

    .daily-section {
        margin-bottom: 40px;
    }

    .logs-list {
        list-style-type: none;
    }

    .logs-list li {
        margin-bottom: 8px;
    }

    .done::marker {
        content: "🟢 ";
    }

    .in-progress::marker {
        content: "🟡 ";
    }

    .fix::marker {
        content: "🔴 ";
    }
</style>

