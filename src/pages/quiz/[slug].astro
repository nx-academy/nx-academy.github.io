---
import { QUIZ_SLUGS } from "../../data/quiz"

export function getStaticPaths() {
    return QUIZ_SLUGS.map(slug => ({
        params: { slug: slug }
    }))
}

import BaseLayout from "../../layouts/BaseLayout.astro"
import QuizIsland from "../../components/Quiz"

const { slug } = Astro.params;

console.log("====")
console.log(slug)
console.log("====")
---

<BaseLayout>
  <main class="main-wrapper">
    <QuizIsland client:load />
  </main>
</BaseLayout>

<style>
    .main-wrapper {
        width: 90%;
        margin-left: auto;
        margin-right: auto;
        max-width: 1100px;
    }
</style>