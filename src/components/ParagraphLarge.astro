---
interface Props {
  content: string;
}

const { content } = Astro.props;
---

<p>
  {content}
</p>

<style>
  p {
    color: var(--textColor);
    font-size: clamp(1rem, 1rem + 0.139vw, 1.125rem);
    margin-bottom: 0;
    margin-top: 0;
  }
</style>
